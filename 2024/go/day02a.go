package main

import (
    "os"
    "fmt"
    "strings"
    "strconv"
    "sort"
)

func main() {
    dat, err := os.ReadFile("day02.txt")

    if err != nil {
        panic(err)
    }

    lines = strings.Split(dat, "\n")
    numLines = len(lines)

    for el, i := range(lines) {
        nums := strings.Split(el, " ")

    }


}
